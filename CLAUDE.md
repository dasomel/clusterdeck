@AGENTS.md

# ClusterDeck Claude adapter

For Discovery, SSH/bootstrap, bastion/ProxyJump, kubeconfig, hosts-file, or connection verification work, load `.agents/skills/clusterdeck-connection-workflow/SKILL.md`.

Keep Claude-only orchestration outside the portable skill; repository boundaries and verification requirements remain in `AGENTS.md`.
